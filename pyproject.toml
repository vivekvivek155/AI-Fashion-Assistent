[project]
name = "ai-fashion-assistant"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "python-multipart",
    "python-dotenv",
    "google-generativeai",
    "pandas",
    "numpy",
]

[tool.vercel]
entrypoint = "backend.main:app"
